-- Drops the blogger if it exists currently --
DROP DATABASE IF EXISTS bloggins;
-- Creates the "blogger" database --
CREATE DATABASE bloggins;

SELECT * FROM bloggins.Authors;
SELECT * FROM bloggins.Posts;